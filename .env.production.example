# Production Environment Variables for Railway
# Copy these values into Railway dashboard → Node.js service → Variables

# Database Configuration (Aiven PostgreSQL)
# This is your existing Aiven database connection
DATABASE_URL=postgres://avnadmin:AVNS_ycOqIJy7HhutnlbHGSQ@ksardb-cmk-25.d.aivencloud.com:17327/defaultdb?sslmode=require

# JWT Secret for Token Authentication
# Generate a secure random key (minimum 32 characters)
# Command: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-a-secure-random-key-minimum-32-characters

# Node Environment
NODE_ENV=production

# Server Port (Railway will assign automatically, but set for consistency)
PORT=3000

# Application URL (for reference/emails)
APP_URL=https://ksarapp.sagafit.es

# Optional: For Vercel Frontend
REACT_APP_API_URL=https://ksarelkebirCases-production.up.railway.app
